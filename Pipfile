[[source]]
url = 'https://pypi.python.org/simple'
verify_ssl = true
name = 'pypi'

[requires]
python_version = "3.6"

[packages]
pytest-relaxed = ">=1.1.0,<2"
invocations = "==0.20.0"
watchdog = ">=0.8.3,<2"

[dev-packages]
invocations = {path = "../../oss/invocations", editable = true}
